# The LLM model to use (e.g. "openai/gpt-4o")
FLUE_MODEL=provider-id/model-id

# SQLite file for Drizzle node:sqlite
DB_FILE_NAME=app.db

# Optional: override the default Flue backend URL
VITE_FLUE_BASE_URL=http://localhost:3583
